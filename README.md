# DocuInsight 🧠

**Chat with your documents without the cloud bill.**

DocuInsight is a local-first RAG tool that keeps your data private and your UI buttery smooth. By splitting the stack (React frontend + Python AI backend), the heavy lifting never blocks the browser.

* 💸 **$0 API Costs:** Runs completely on local embeddings (`all-MiniLM-L6-v2`).
* ⚡ **No Lag:** Decoupled architecture for fluid interactions.
* 🛠️ **Stack:** React, FastAPI, ChromaDB.
